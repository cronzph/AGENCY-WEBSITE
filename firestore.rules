rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Open access — all collections readable and writable
    // Security is handled at the application level (admin-only UI, no public write exposure)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
